{
  "scripts": {
    "test": "jest",
    "dev": "npx firebase emulators:start",
    "deploy-hosting": "npx firebase deploy --only hosting",
    "deploy-functions": "npx firebase deploy --only functions",
    "deploy-all": "npx firebase deploy",
    "deploy-qa": "npx firebase hosting:channel:deploy qa"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  }
}